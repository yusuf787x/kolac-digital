rules_version = '2';

// Single-user dashboard: any authenticated user with a valid Firebase Auth
// session can read and write all collections. Since you only invite yourself
// as a user, this is safe.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

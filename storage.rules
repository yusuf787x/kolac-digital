rules_version = '2';

// Single-user dashboard: any authenticated user can read/write any path.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
